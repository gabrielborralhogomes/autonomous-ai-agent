# Images

Place screenshots and demo images here.

Suggested images to add:
- `agent_run_terminal.png` — terminal output showing a full agent run with token report
- `plan_execute_flow.png` — screenshot of the plan JSON being generated
- `web_automation_demo.gif` — screen recording of the agent navigating a web portal
- `cost_report.png` — screenshot of the token cost report at the end of a run
